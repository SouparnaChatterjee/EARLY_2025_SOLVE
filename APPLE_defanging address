class Solution {
public:
    string defangIPaddr(string address) {
    string res;
    for(auto& it:address)   
    {
        if(it=='.')
        res+="[.]";//double quotes
        else
        res+=it;
    } 
    return res;
    }
};
